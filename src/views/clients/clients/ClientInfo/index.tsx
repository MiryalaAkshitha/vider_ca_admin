import { Typography } from "@mui/material";

function KybInfo() {
  return (
    <Typography sx={{ p: 3 }}>
      Should be integrated with new form builder
    </Typography>
  );
}

export default KybInfo;
